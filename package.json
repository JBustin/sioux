{
  "name": "docker",
  "version": "1.2.1",
  "description": "Mount and test vhost (rewrite rules)",
  "main": "lib/index.js",
  "scripts": {
    "start": "node lib/index",
    "generate:hosts": "node lib/generate-hosts",
    "generate:conf": "node lib/generate-conf",
    "lint": "eslint --ext .js --ignore-path .eslintignore .",
    "prettier": "prettier --write 'lib/**/*.js' '*.js'"
  },
  "author": "jbustin",
  "license": "ISC",
  "dependencies": {
    "abort-controller": "^3.0.0",
    "chai": "^4.2.0",
    "dotenv": "^8.2.0",
    "node-fetch": "^2.6.1",
    "prettier": "^2.2.1"
  },
  "devDependencies": {
    "eslint": "^7.11.0"
  }
}
